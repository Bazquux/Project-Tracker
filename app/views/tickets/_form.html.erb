<%= semantic_form_for [@project, @ticket] do |f| %>
	<%= f.inputs do %>
		<%= f.input :name , :label => "Ticket Name" %>
		<%= f.input :body , :label => "Ticket Description" %>
	<% end %>
	
	<%= f.buttons %>
<% end %>
