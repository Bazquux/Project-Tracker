<%= semantic_form_for(@project) do |f| %>
	<%= f.inputs do %>
		<%= f.input :name , :label => "Project Name" %>
		<%= f.input :body, :label => "Project Description" %>
	<% end %>
	
	<%= f.buttons %>

<% end %>
